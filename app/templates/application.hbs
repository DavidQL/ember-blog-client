<div class="container">
	<div class="page-header">
		<h1>
			{{#link-to 'index'}}Bloggidy blog {{/link-to}}</h1>
	</div>

	<div class="row">
		<div class="sidebar col-md-3">
			Authors:
			<ul>
				{{#each authors}}
					<li>
						{{#link-to 'author' id}} {{name}} {{/link-to}}
					</li>
				{{/each}}
			</ul>
		</div>
		<div class="main col-md-9">
			{{outlet}}
		</div>
	</div>
</div>
